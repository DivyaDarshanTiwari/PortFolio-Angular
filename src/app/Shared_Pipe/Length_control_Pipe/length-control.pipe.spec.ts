import { LengthControlPipe } from './length-control.pipe';

describe('LengthControlPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthControlPipe();
    expect(pipe).toBeTruthy();
  });
});
